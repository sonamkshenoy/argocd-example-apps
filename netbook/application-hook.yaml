apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: frontpage
spec:
  replicas: 2
  selector:
    matchLabels:
      page: front
  template:
    matadata:
      labels:
        page: front
    spec:
      containers:
        - name: main
          image: nginx:latest
